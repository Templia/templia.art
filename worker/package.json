{
  "name": "templia-markdown-for-agents",
  "version": "0.2.0",
  "private": true,
  "description": "Cloudflare Worker on templia.art/*. Routes: /api/availability (JSON availability parsed from Airbnb-synced iCal). Content negotiation: Accept: text/markdown turns HTML responses into markdown via Workers AI toMarkdown, reproducing the native Markdown for Agents feature on the Free plan.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
