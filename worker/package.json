{
  "name": "templia-markdown-for-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that negotiates Accept: text/markdown for templia.art, reproducing the native Markdown for Agents feature on the Free plan.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
